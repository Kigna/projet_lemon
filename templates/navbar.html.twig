<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <div class="navbar-brand text-warning mx-5">
            <img src="{{ asset('assets/img/logoLemon.png') }}" alt="logo" width="70" height="70" class="d-inline-block align-text-center">
            <span class="text-warning">Gestion d'événements</span>
        </div>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ml-5"> {# Utilisez "ml-5" pour ajouter un espace à gauche #}
                <a class="nav-link active" aria-current="page" href="/">Accueil</a>
                
            </div>

        </div>
         <div class="navbar-nav d-flex ml-auto"> {# Utilisez "ml-auto" pour aligner à droite #}
                <ul class="navbar-nav d-flex ml-auto">
                    {% if app.user%}
                        <li class="nav-item">
                            <a class="nav-link" href="/evenements">Mes événements</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/add_evenement">Ajouter d'évenements</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-info mx-3" aria-disabled="true" href="{{path('app_logout')}}">Se déconnecter</a>
                        </li>
                        
                    {% else %}   
                        <li class="nav-item">
                            <a class="nav-link text-info mx-3" aria-disabled="true" href="{{path('app_login')}}">Se connecter</a>
                        </li>  
                        <li class="nav-item">
                            <a class="nav-link text-info mx-3" aria-disabled="true" href="{{path('app_register')}}">S'inscrire</a>
                        </li> 
                    {% endif  %}
                   
                </ul>
                
            </div>
    </div>
</nav>